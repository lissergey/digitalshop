<h2>Редагувати профіль</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>
    <div class="form-group">
      <%= f.label "Ім'я" %><br />
      <%= f.text_field :name, autofocus: true, class: "form-control" %>
    </div>
  <div class="form-group">
    <%= f.label :email %><br />
    <%= f.email_field :email, class: "form-control" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="form-group">
    <%= f.label 'Пароль' %> <i>(пропустіть це поле якщо не хочете змінити пароль)</i><br />
    <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label 'Повторіть пароль' %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label 'Дійсний пароль' %> <i>(необхідно ввести дійсний пароль для підтверждення змін)</i><br />
    <%= f.password_field :current_password, autocomplete: "off", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit "Оновити", class: "btn btn-success" %>
  </div>
<% end %>

<h3>Видалення аккаунта</h3>

<p>Хочете видалити свій аккаунт? <%= button_to "Видалити аккаунт", registration_path(resource_name), data: { confirm: "Ви впевнені що хочете видалити свій аккаунт?" }, method: :delete, class: "btn btn-danger" %></p>

<nav>
  <ul class="pager">
    <li><%= link_to 'Назад', :back, class: "previous"%></li>
  </ul>
</nav>